---

- name: Creates router, network, and host aggregation
  hosts: controller
  roles:
    - role: post_install/openstack_tasks
      setup_os_env: true
      create_networks: true
      images_upload: true
      overcloud_delete: false
