---

- name: Prepare the environment
  hosts: undercloud
  gather_facts: False
  vars:
    image: false
    keypair: true
    security_group: true
  roles:
    - post_install/openstack_tasks

- name: Tempest deployment and execution
  hosts: tester
  gather_facts: False
  roles:
    - tester/tempest
