---

- name: Rally test
  hosts: "{{ tester | default('undercloud') }}"
  gather_facts: False
  vars:
    ansible_python_interpreter: /usr/libexec/platform-python
  roles:
    - role: roles/tester/rally
