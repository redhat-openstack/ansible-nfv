---
collections:
  - name: containers.podman
  - name: ansible.posix
  # This is the latest supported collection for openstacksdk > 1.0.0
  # DO NOT use newer collection, this will cause unexpected issues
  - name: openstack.cloud
  - name: kubernetes.core
