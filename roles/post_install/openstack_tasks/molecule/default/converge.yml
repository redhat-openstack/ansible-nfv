---
- name: Converge
  hosts: "{{ osp_tasks_host | default('undercloud') }}"
  gather_facts: false
  # vars_files:
    # - roles/openstack_tasks/molecule/vars/main.yml
  roles:
    - role: roles/post_install/openstack_tasks
      resource_state: present
